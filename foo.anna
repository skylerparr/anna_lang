defmodule(Foo, {
  @alias vm.Pid;

  @def bar([Pid], {
    Kernel.spawn(@_'Foo', @_'repeat');
  });

  @def repeat([Atom], {
    Kernel.sleep(1000);
    System.println('counting');
    repeat();
  });
});